import { Box, Typography } from '@mui/material';
import * as React from 'react';
import { CounterButton } from 'ui';

export const IndexPage = () => {
   return (
      <Box>
         <Typography variant="h3">Websight</Typography>
         <CounterButton />
      </Box>
   );
};
